# ─── СЕКРЕТЫ И РАНТАЙМ — НИКОГДА НЕ КОММИТИТЬ ───────────────────────────
# session.db = токен доступа к личному аккаунту MAX (полный доступ!)
**/session.db
**/session.db-*
# реальный конфиг содержит телефон — в репо только config.example.json
sidecar/config.json
config.json
# очередь и логи
**/pending.json
*.log
bridge-watchdog.log

# ─── venv / кэши ───────────────────────────────────────────────────────
.venv/
venv/
__pycache__/
*.pyc
*.pyo

# ─── разное ────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
